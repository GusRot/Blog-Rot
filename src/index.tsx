import React from "react";
import ReactDOM from "react-dom";
import DarkThemeWithCustomPalette from "./theme";

ReactDOM.render(
    <React.StrictMode>
        <DarkThemeWithCustomPalette />
    </React.StrictMode>,
    document.getElementById("root")
);
